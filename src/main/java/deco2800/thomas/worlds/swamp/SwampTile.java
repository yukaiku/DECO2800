package deco2800.thomas.worlds.swamp;

import deco2800.thomas.worlds.Tile;

public class SwampTile extends Tile {
    public SwampTile(String texture, float col, float row) {
        super(texture, col, row);
    }
}
